# Agent Instructions — ai-ralph

## Repository

Fork of [frankbria/ralph-claude-code](https://github.com/frankbria/ralph-claude-code).
Owner's fork: **`amit-t/ai-ralph`**.

## Pull Requests

Always target `amit-t/ai-ralph`. Never open PRs against the upstream repo.

```bash
gh auth switch --user amit-t
gh pr create --repo amit-t/ai-ralph --base main --head <branch>
```
